---
# vars file for roles/docker

docker_apt_architectures:
  x86_64: amd64
  aarch64: arm64
  armv7l: armhf
  armv6l: armhf
  ppc64le: ppc64le
  s390x: s390x

docker_apt_architecture: "{{ docker_apt_architectures[ansible_architecture] | default(ansible_architecture) }}"
